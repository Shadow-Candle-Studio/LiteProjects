# 关卡编辑器
关卡以json格式存储。

## 代码路径
LevelEditor

## 名词
- table:桌面
- wall:墙
- coin:硬币。
- block:障碍物。类似墙。可反弹硬币。
- mud:陷阱。拥有和桌面不同的摩擦力。


## 界面
title:CoinDuel2D Editor
顶部：主菜单
左边：对象列表
中间：渲染桌面
右边：属性栏

### 主菜单
- File:
主要控制关卡配置文件的新建/打开/保存
1，新建
2，打开
3，保存
4，另存为
5，关闭

### 对象列表
根节点：桌面
子节点：墙（不可删除）
如果新建其他对象，做为根节点的子节点
如果选中某个对象，属性栏显示该对象的属性。
如果选中的是非桌面对象，在中间渲染窗口显示选中对象的包围盒。

### 属性栏
当选中某个对象时，属性栏显示该对象属性。包括位置等。修改属性，立刻生效。

### 渲染桌面
当没有创建桌面是，显示深色。
当创建桌面后，读取res/table_1.png渲染桌面。
当桌面被渲染，同时要渲染墙。墙是围着桌面的。墙有厚度，墙根据桌面的尺寸向内扩展。渲染墙时使用res/wall_1.png
根据当前level的数据渲染桌面和对象。
点击对象，让对象进入选中状态。同步更新对象列表的选中状态，和属性栏显示的内容。

#### 渲染桌面右键菜单
- 添加
1，硬币
2，障碍物
3，陷阱
- 删除
如果鼠标能选中某个对象，删除选项激活
物体支持拖动



## 技术栈
python+pyside6
## level配置文件参考
路径：LevelEditor/level1.json
